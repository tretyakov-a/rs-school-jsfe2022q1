export enum HttpCodes {
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
}

export enum FILTER_NAME {
  CATEGORY = 'category',
  COUNTRY = 'country',
  LANGUAGE = 'language',
  SEARCH = 'search',
}

export const DEFAULT_FILTER_OPTION = 'all';