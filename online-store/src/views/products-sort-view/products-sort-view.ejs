<div class="select">
  <div class="select__title">Сортировать</div>
  <select class="select__input" name="products-sort">
    <% Object.keys(locals.options).forEach(function(key) { %>
      <option value="<%= key %>"><%= locals.options[key][0] %></option>
    <% }); %>
  </select>
</div>