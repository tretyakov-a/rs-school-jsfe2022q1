<a class="button button_circle top-button" href="#top">
  <i class="fas fa-arrow-up"></i>
</a>
<header class="header <%= locals && locals.page %>">
  <div class="header__container container">
    <a class="logo" href="<% if (locals.page === 'header_pets') { %> ./ <% } else { %>#<% } %>">
      <h1 class="logo__title">Cozy House</h1>
      <div class="logo__subtitle">
        Shelter for pets in Boston
      </div>
    </a>
    <nav class="header__nav">
      <div class="header__menu-container">
        <ul class="header-menu">
          <li class="header-menu__item">
            <a href="<% if (locals.page === 'header_pets') { %> ./ <% } else { %>#<% } %>" class="header-menu__link <% if (locals.page === 'header_main') { %> header-menu__link_active <% } %>">About the shelter</a>
          </li>
          <li class="header-menu__item">
            <a href="<% if (locals.page === 'header_main') { %> ./pets.html <% } else { %>#<% } %>" class="header-menu__link <% if (locals.page === 'header_pets') { %> header-menu__link_active <% } %>">Our pets</a>
          </li>
          <li class="header-menu__item">
            <a href="./#help" class="header-menu__link">Help the shelter</a>
          </li>
          <li class="header-menu__item">
            <a href="#footer" class="header-menu__link">Contacts</a>
          </li>
        </ul>
      </div>
      <button class="burger">
        <span class="burger__line"></span>
        <span class="burger__line"></span>
        <span class="burger__line"></span>
      </button>
    </nav>
  </div>
</header>
